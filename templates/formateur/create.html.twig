{% extends 'base.html.twig' %}


{% block body %}

<h1> Création d'un article ! </h1>

{{ form_start(formUser) }}

	{{form_row(formUser.username, {'attr':{'placeholder':"Titre de l'article"}}) }}
	{{form_row(formUser.password, {'attr':{'placeholder':"Titre de l'article"}}) }}
	{{form_row(formUser.nom) }}	
	{{form_row(formUser.prenom, {'attr':{'placeholder':"Contenu de de l'article"}}) }}
	{{form_row(formUser.type, {'attr':{'placeholder':"Image de l'article"}}) }}

	<button type="submit" class="btn btn-success"> 
		{% if editMode %}
			Enregistrer les modifications
		{% else %}
			Ajouter l'utilisateur
		{% endif %}

	</button>
{{form_end(formUser)}}

{% endblock %}